# LeetFlash

Minimal Expo web app for swiping through LeetCode flashcards with Python solutions, solve notes, and a typing practice area.

## Run

```bash
npm install
npm run api
npm run web
```

The app runs on Expo web. The local API proxy fetches LeetCode problem prompts and starter signatures.
